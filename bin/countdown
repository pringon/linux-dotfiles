#!/bin/sh

case $1 in
    ''|*[!0-9]*) duration=60 ;;
    *) duration=$(( "$1" * 60 )) ;;
esac

while [ $duration -gt 0 ] 
do
  echo "$(( duration / 60)):$(( duration % 60))"
  : $(( duration = duration - 1))
  sleep 1
done

mpv ~/bin/bell.mp3
